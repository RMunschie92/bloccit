New post and favorite  "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

You will now be notified of all updates to "<%= @post.title %>".  

Visit online at: <%= topic_post_url(@post.topic, @post) %>
